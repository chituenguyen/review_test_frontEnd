import logo from './../../assets/img/logo.png'
import './styles.scss'
const Logo = () => {
  return (
    <img
      alt="logo"
      className="logo"
      src={logo}
    />
  );
};

export default Logo;
